# Tests Workspace

Future home for cross-project tests, integration tests, fixtures, and validation assets.

Runtime-specific unit tests may live near their implementation once frontend and backend workspaces exist. Shared behavior and end-to-end validation can be organized here when those workflows are introduced.
